#include <stdio.h>
#include <unistd.h>
int main(int argc, char const *argv[]) {
  int index;
  for(index=0;index<100;index++)
  {
    printf("the value of i is -- %s\n",i );
    sleep(1);
  }
  return 0;
}
